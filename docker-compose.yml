version: '3.9'

services:
  api:
    build: ./api
    container_name: monitoring_api
    ports:
      - "8000:8000"
    env_file:
      - .env

  bot:
    build: ./bot
    container_name: telegram_bot
    depends_on:
      - api
    env_file:
      - .env